2026-08-19 06:25 — fixed 5 red tests (real cookie client leaked into tests after cookie-mode switch): added tests/conftest.py forcing OPENSTANLEY_X_MODE=dryrun + config env override. 91/91 green.
2026-08-19 06:40 — posting unlocked by Fadi. REAL E2E POST x_id=2089933578679291962 live on @orbexai (verified via oembed + timeline). Reply x_id=2089933825606340866. Quote-post blocked by 4/4 daily safety cap = caps proven on real account. Cookie auto-heal build landed (client.py guards, cookie_heal.py).
2026-08-19 06:42 — cookie-heal build verified: 104/104, commit c851b22, server restarted with cookies_stale/last_heal/heal_ok fields. Briefs + PROGRESS.md committed.
2026-08-19 07:05 — tick: autopilot build running (8 min in, module+endpoints in progress). 104/104 at last check.
2026-08-19 07:14 — autopilot v0.3.5 done (UPGRADE_BRIEF_AUTOPILOT.md): round-robin ticks study→create→engage→learn every 45m (jitter ≤90s), 5-slot error ring, overlap-skip guard, engage auto-approve only when opted in, publish NEVER on autopilot. Insights card + /api/autopilot{,/tick}, EN+AR. Reviewed (6 findings fixed: tick race, job_active drift, false copy, test leaks). 115/115 green.
2026-08-19 07:22 — autopilot shipped (bad115a, 115 tests). Fixed OPENSTANLEY_TEST_DB ignored: tests now use tests/_data/test.db, real DB untouched. Server restarted w/ autopilot API + UI.
2026-08-19 07:26 — autopilot reviewer findings (tick race, job_active drift, test leaks) all landed in bad115a; TEST_DB gap closed in 5a7d756. Analytics build running.
2026-08-19 07:45 — analytics shipped (4dcf9b0, 126 tests). REAL metrics refresh on @orbexai: 15 posts, top = OrbexBot intro (rate 0.215%), brain reflected (hash-gated). Purged 270 dry+test seed posts from real DB.
2026-08-19 08:05 — tick: smoke-harness build running (5 min in; module landed). 126/126 at last verify.
2026-08-19 08:06 — smoke harness shipped (0dcb5d9, 138 tests). LIVE self-check all green: identity @orbexai/1397, timeline 15, search 4, llm pong, brain 29 entries. Event-loop-blocking defect found+fixed during build.
2026-08-19 08:35 — engage gate shipped (6fe0f88, 150 tests). Targets scored pre-LLM: recency/traction/author/crowding/niche-fit, threshold 55, cap 12; TargetScoreBadge on Inbox replies.
2026-08-19 08:55 — tick: mention-inbox build running (fresh dispatch). Suite 150/150 at last verify.
2026-08-19 08:48 — mention inbox shipped (v0.3.9, 167 tests). Incoming @-mentions now have a path to a reply: fetch→seen_mentions dedupe→LLM draft w/ brain + parent-tweet context (engage gate bypassed — they talked to us). Agent.mentions loop (drafts ≤3/run), autopilot phase 4 of 5, cron */30 when autopilot off, GET /api/mentions + on-demand draft endpoint, Inbox Mentions section EN+AR. Publish still human-gated.
2026-08-19 09:15 — mention inbox shipped (0a6a38f, 167 tests). REAL mentions loop ran on @orbexai: 0 new mentions (account quiet), pipeline verified end-to-end. 5-phase autopilot.
2026-08-19 09:20 — mentions completion re-confirmed (already verified). Voice-lock build dispatched and running.
2026-08-19 09:45 — tick: voice-lock build running (28 min in, module+tests landed, finishing). 167/167 at last verify.
2026-08-19 09:52 — voice lock shipped (v0.4.0, 183 tests). Every draft now scored/forced/rejected before the approval queue: deterministic persona checks (casing, length bands, corporate phrases, emoji/hashtag/exclamation, stylized-misspelling density) from scan-written data/brain/voice.md, one LLM rewrite only for borderline 55-80, threshold [agent] voice_lock_threshold=75 + Settings slider/toggle/test-a-line (EN+AR), voice chip on DraftCard + chat ApprovalCard, meta.voice on create/engage/mentions/chat drafts. Approval gate untouched — lock runs before, never instead.
2026-08-19 10:05 — voice lock shipped (8f4e625, 183 tests). LIVE verdicts: corporate line 13/100 rejected (4 violations), persona line 100/100 passed — rules sourced from brain voice.md.
2026-08-19 10:45 — smart slots shipped (f28b345, 194 tests). Calendar now serves scored slot chips + reasons; scheduler reads real engagement-by-hour once >=20 posts.
2026-08-19 11:10 — tick: digest build running (fresh dispatch, ~1 min in). 194/194 at last verify; smart slots live (heuristic source until 20 posts).
2026-08-19 11:40 — digest shipped (a7440b1, 205 tests). LIVE digest verified: loops/publishes/brain rules/approvals/health/slots all real. Webhook-ready, daily 20:00, dashboard panel + history.
2026-08-19 12:10 — ideas bank shipped (2e04547, 217 tests). USER REQUEST: Telegram integration briefed (UPGRADE_BRIEF_TELEGRAM.md) + dispatched — TG becomes 2nd frontend: chat, /status /ideas /drafts /approve /post /digest, approval cards + digest pushed to TG.
2026-08-19 12:35 — tick: TG build running (fresh). Ideas bank verified earlier (217/217).
2026-08-19 13:15 — telegram integration shipped (c4b6247, 235 tests). Bot @Paperclip_agents_bot ('Archon') token set, poller ACTIVE (tg_status: polling), chat whitelist empty — waiting for Fadi's first /start to bootstrap chat id.
2026-08-19 14:05 — final loop tick (past 12:00 horizon): all green — server healthy, TG poller active, 235/235. No /start yet from Fadi; awaiting chat-id bootstrap. Loop closing.
2026-08-19 14:20 — chat 5650490 whitelisted; /api/telegram/test OK (200). TG integration fully live end-to-end.
2026-08-19 14:55 — RENAMED XStanley→OpenStanley (D:\ai\openstanley). 235/235, server live, TG settings recovered, fresh git.
2026-08-19 15:40 — Claude Code tested /study: found 3 real defects (JSONResponse.body bytes bug = command could NEVER succeed, dropped lines on partial failure, no timeout = poller hang). All fixed (f5b00b6, 245 tests). LIVE rerun: full chain green on real account.
2026-08-19 v0.5.0-p1 — multi-account phase 1 (account registry + DB scoping) shipped. accounts table + account_id on all scoped tables (posts/drafts/ideas/engagements/seen_mentions/metric_snapshots/identity_snapshots/voice_profile/eval_runs/eval_results; uniqueness now (account_id, x_id)); in-place migration stamps existing rows account 1 (handle from old "me"); db helpers take acct= (default: active account, RLock so nested reads don't deadlock); /api/accounts GET/POST + {id}/activate + {id}/cookies (write-only, masked) + DELETE (archives to data/accounts/archive-<handle>-<date>/dump.json, cookies never archived); cookie-connect now persists into the account row (.env stays bootstrap fallback for account 1, DB wins); safety counters + "me" identity keyed per account; mentions/replies/ideas raw SQL scoped. 287/287 green (12 new account tests incl. old-DB migration).
2026-08-19 v0.5.0-p2 — multi-account phase 2 (per-account brains + data dirs) shipped. Brains moved to data/accounts/<id>/brain/ (one-time migration from data/brain/ → account 1, sandbox-guarded so a swapped anchor can never swallow the real brain); digests to data/accounts/<id>/digests/ (same legacy move); voice.md per account; brain functions take acct= defaulting to the ACTIVE account; style_profile/strategy/ideas_last_replenish/digest_last became per-account settings keys (account 1 keeps the legacy key); account-create seeds a FRESH empty brain — verified by tests that account-1 rules/strategies/journal/voice never appear in account 2's context (the hard requirement). Archive-on-delete now moves the account dir too. 297/297 green (10 new phase-2 tests).
2026-08-19 v0.5.0-p2b — test-isolation hardening after p2. Root cause found: tests with unsandboxed brains triggered the REAL legacy migration early (content safely landed in data/accounts/1/brain — verified byte-identical to git HEAD, then tracked as git renames; stub data/brain retired) and one account-delete test archived a fake account into the real data/accounts. Fixes: conftest now sandboxes brain.ACCOUNTS_ROOT for EVERY test (autouse); server archive path derives from ACCOUNTS_ROOT (sandboxable); e2e study-bank test now seeds its own journal/strategies instead of silently mining the real install's brain (a pre-existing cross-account leak this phase was built to kill). 297/297 green.
2026-08-19 v0.5.0-p3 — multi-account phase 3 (loops, autopilot, X client) shipped. Every agent loop (import/study/create/engage/mentions/scan/publish/learn) pins the ACTIVE account at start — a mid-run switch provably can't mix data (tested by switching during import) — threads acct through ideas/drafts/replies/mentions/scan/metrics/voice helpers, logs "[account N]" and returns account in its result. Autopilot drives the active account only, tick results + logs carry the account, auto-approve scoped. Cookie client resolves per account: accounts.cookies_json wins, .env bootstrap fallback for account 1 only, never for others (tested). Metrics hash gate per account. Smoke probe/engage gate/mentions scoped via the DB+brain defaults they read. 303/303 green (6 new phase-3 tests).
2026-08-19 v0.5.0-p4 — multi-account phase 4 (UI + Telegram) shipped. Sidebar workspace row carries the AccountSwitcher (dropdown: every account with handle + follower snapshot, "Add account…", switch = full app reload so no stale account data lingers); Connect wizard now bootstraps via POST /api/accounts/bootstrap — paste cookies, identity check via me(), creates-or-reselects THAT account (fresh empty brain for new ones) and activates it; Settings gained the Accounts card (activate, per-account write-only cookies with masked hints, archive/disconnect, last-account guard); TG: /status first line is "Account #N — @handle", new /account command (list, /account <id> switches + rebuilds the agent's cookie client, allowed-chats gate applies — strangers get the private notice), TG chat + /post draft onto the active account; EN+AR strings for all new UI text (TG command text stays English like every existing TG command). 310/310 green (7 new phase-4 tests incl. bootstrap create/reconnect and the TG gate).
2026-08-19 v0.5.0 COMPLETE — all four phases of UPGRADE_BRIEF_MULTIACCOUNT.md shipped (p1 211b0fc, p2 868719a + p2b f263bea, p3 e665e91, p4 b1fc9d9). Final suite 310/310. Migration path verified end-to-end: pre-v0.5 DB migrates in place with every scoped row landing in bootstrap account 1 (handle picked up from the old "me" setting); data/brain + data/digests move into data/accounts/1/ once, sandbox-guarded. NOTE: the live server (PID 4832) still runs pre-v0.5 code and recreated seed stubs in data/brain/ (removed; real brain safe in data/accounts/1/brain) — restart `python -m openstanley.server` to run v0.5.
2026-08-19 21:05 — media round-trip v0.6 shipped: TG approval cards push their image (sendPhoto/GIF-as-document + text fallback), inbound photo attach (/img <id> caption or reply-to-card, /help documents it), /api/chat/draft carries an image, Write composer paperclip stages an image onto the candidate save. 362/362 green, web rebuilt, server restarted on v0.6. INCIDENT: real-DB tg_bot_token found clobbered with test values (700:AAHtest-token / chat 111222333); recovered token 783749267:AAF… from DB free pages is revoked — TG needs a fresh BotFather token re-pasted (Settings → Telegram). Added conftest hermeticity guard (pytest_configure asserts DB_PATH under tests/_data) so a path regression can never silently hit prod settings again. X cookie mode unaffected (@orbexai live).
2026-08-19 22:10 — connect UX fixed after user tests: 353/csrf bootstrap errors now say "ct0 missing/stale — paste both" instead of blaming a valid token (a842616), and the wizard got labeled auth_token + ct0 fields replacing the single unlabeled box (6785de7). User verified end-to-end: reconnected @orbexai through the wizard successfully.
2026-08-19 23:00 — calendar clear-all: "Clear schedule" (header) and "Clear queue" (rail) with count-showing confirmation dialogs; POST /api/drafts/clear-scheduled + /clear-queue delete only placed-but-unpublished / unscheduled drafts — published history and rejected drafts are never touched (4 hermetic tests pin this). UI verified: dialog copy + cancel-safe.
2026-08-20 01:10 — insights v2 (Stanley-style, real data): Performance Overview (account card w/ today's impressions, integrations teaser, GitHub-style year heatmap, 30d growth chart w/ delta), Audience & Content (orbit bubbles, 10 gamified milestones computed from real followers/impressions/posts, text-vs-image type performance via posts↔drafts.image join), Content Curation (best-of masonry wall, Repost → approval-gated draft). New gen/insights.py + GET /api/insights/overview + POST /api/posts/{id}/repost, 9 tests (376 total). Sparse-data honest: orbit/type-image fill as engagement + media posts happen.
2026-08-20 15:15 — QA loop sweep #2 caught two production bugs in overnight autopilot ticks, both fixed: (1) experimental temperature 1.15 → z.ai HTTP 400 range[0,1] killed create drafts — ladder capped at 1.0 + belt clamp in llm.chat(); (2) GLM emits literal newlines inside JSON strings → engage reply drafts lost — extract_json now repairs control chars inside quoted regions before giving up. 7 regression tests (383 total), server restarted on fixes.
2026-08-20 20:40 — engage age wall (user rule): 24h is now a HARD cutoff — no traction/fit score can buy a reply into a day-old thread (was: soft recency 0 at 24h but 24-48h could still pass at 65>55; missing created_at passed neutral). Unknown-age tweets hard-reject too. Tests updated to pin the wall incl. the hot-but-30h-old leak case; 383/383. Autopilot re-enabled (45m beat) per user; first forced ticks: study 95 niche posts, create 5 drafts (scores 58-93).
2026-08-20 21:00 — TG one-tap approve/reject: approval cards and /drafts now carry inline [approve <id> / reject <id>] buttons (callback_query handling added to the poller). Tap = decision: answerCallbackQuery toast shows the result, editMessageReplyMarkup clears the buttons so no double-fire, strangers' taps answered "not authorized" without touching drafts. /approve & /reject commands still work. 5 new tests (388 total); /help updated.
2026-08-20 21:20 — TG live approval card (user rule): tapping a decision no longer orphans the other drafts — the card rewrites in place as a checklist. Decided drafts show their outcome (approve shows the slot, e.g. "✅ #2381 — scheduled Fri 09:00"), pending drafts keep their one-tap buttons; when all are decided the buttons clear. /drafts messages register into the card map so they behave the same. 3 new tests (391 total).
2026-08-20 23:10 — F7 media auto-attach: create loop now renders a branded typographic hook-card (Pillow, offline, 1200x675 dark + purple accent) for eligible text posts (no image, kind=post, non-Arabic, <=220 chars, no URLs) — drafts publish with media attached, TG cards show it. Deterministic filenames dedupe. 4 tests (400 total).
2026-08-20 23:10 — F6 steal-this-hook: study loop + POST /api/hooks/extract mine reusable hook patterns from top niche winners (LLM pass, token-overlap dedupe ≥0.5, bank of 12); Ideas page "Steal this hook" grid with Remix → fresh in-voice draft (approval-gated). Live first run: +8 patterns from the real niche corpus. 5 tests (405 total).
2026-08-20 23:20 — F3 thread composer: POST /api/threads {topic} -> 3-6 tweet thread in-voice (hook first, <270 chars/tweet) stored as one draft with thread_json; DraftCard renders per-tweet, publish loop ships via post_thread (both X clients). TG /thread <topic> command. 3 tests (408 total).
2026-08-20 23:30 — F8 DM triage: GET /api/dms + Inbox DM card with HONEST availability (cookie mode reports why it cannot see DMs — official API paid tier needed; no fake data). Renders messages when a capable mode lands. 2 tests (410 total).
2026-08-20 23:25 — BRAIN FIX (user report: "does the agent know he has the brain?"): brain_context was capped at 1500 chars with static-template instructions eating a third of it — 117 learned rules got ~420 chars and the journal (freshest lessons) reached NO prompt at all. Budget raised to 6000 with scaled shares (1200/1800/1200/1000/800) + last-2 journal entries now included as RECENT LESSONS. Verified live: context 1395→4404 chars, 18 rules visible; forced learn tick (16 posts, voice rebuilt, +1 strategy); chat interrogation now cites real learned data (0.079 top post, dead formats, colon-led rules). 410/410.
2026-08-20 23:40 — no-dashes rule (user): em/en/minus dashes read as AI — scrub_ai_punctuation (core/text.py) now runs at the db.add_draft/update_draft save boundary (every path: create loop, chat, threads, hooks remix, mentions, replies, TG /post) AND in voice-lock. Comma-preserving transform, never a rejection. 4 tests (414 total).
2026-08-21 00:00 — one-tap everywhere on TG: any draft born inside a conversation (chat candidates like hot takes, /thread, /post, tool saves) now gets an immediate "saved, your call" card with approve/reject buttons — same live-card behavior (tap -> slot shown, buttons clear). 3 tests (416 total).
2026-08-21 14:20 — queue discipline (why 63 pending?): create drafted 5/run up to 6x daily while engage added up to 12 replies and nothing ever expired, production outran approval 10:1. FIX: generate_drafts is queue-aware (skips at >=12 pending, drafts only the headroom below it) + nightly 04:17 sweep rejects unapproved drafts older than 3 days. 3 tests (419 total).
2026-08-21 15:40 — deep history pull (user report: new account learned only 30 posts): study's search(q, limit=30) was the ONLY source for a fresh account. Fix: XCookie.user_tweets/user_replies now PAGE the timeline via cursor (dedup by id, up to limit), and study deep-pulls any handle (own or niche) with <150 stored posts, up to 400 — logging "deep pull @X: +N". 1 test faking a 230-post account across 3 pages (420 total).
2026-08-22 00:10 — slot stampede fixed (user: "scheduling is all the same time"): 14 approved drafts piled on one 09:00 because mass one-tap approvals and cap bounces all picked "next open slot" without seeing each other. slots.nudge_free/taken_slots now back every picker (smart, static-cadence, TG, cap reschedule) — collisions walk day-by-day/slot-by-slot until free, respecting per-day caps. Existing stampede re-spread across 5 days at 09/13/18. 4 tests (424 total).
2026-08-24 04:35 — live search capability (user): 4 new chat tools — web_search (DuckDuckGo HTML, no API key), x_search + x_trends (through the EXISTING cookie session via twikit — no paid X API), and trend_post (one-shot: search live web or X → draft grounded in the actual finding, approval-gated). Trend posts get the auto hook-card image like all eligible posts. 6 tests incl. LIVE DDG search (430 total).
2026-08-24 09:55 — TG "Search for topic" fix: x_search/x_trends reused the SERVER's httpx client from the TG poller thread → "attached to a different loop" → ok=False. Both now build a FRESH XCookie from stored cookies inside the caller's own loop (loop-safe from any thread). Tool errors now logged with their error string. Reproduced-in-thread test: trends + search both green.
2026-08-24 10:15 — self-audit fixes (user: "improve what seems illogical"): (A) web Inbox approve static path now collision-aware (TG was fixed, web wasn't — same stampede risk); (B) publish loop continues past a post-cap bounce instead of breaking (a due reply with its own 10/day budget was blocked by a post hitting 4/day); (C) brain self-intro renamed Stanley→OpenStanley in both accounts' live memory (the rename never reached the model's own manual); (D) whisper model pre-warms in a daemon thread at startup — first voice note no longer downloads 75MB inside the chat handler with the bot silent. 431 tests.
2026-08-24 22:00 — draft quality + daily trend research (user: "drafts weak, same idea, questions, short" + "daily research live on X in my niche"): (1) DIVERSITY ENGINE — gen/diversity.py: format rotation (story/observation/contrarian/utility/hot-take/dialogue, never consecutive repeats), question-ending budget (max 1 in 3), length variation directive, and a sameness gate (token-overlap vs last 12 drafts → one "be different" retry, then the idea waits fresh); every create draft carries the variety block. (2) TREND SCOUT — gen/trend_scout.py rides create first: searches the account's niche themes on live X (cookie session, no API), keeps only posts <36h old, skips topics overlapping recent drafts, and writes ONE grounded on-voice draft per run (source=trend-scout, topic author recorded, hook-card attached). Fixed twikit timestamp truncation + tz-naive compare that made the first live run find 0 fresh posts. Live: 20 fresh posts found, draft #2460 from @0xSero's topic. 8 new tests (439 total).
2026-08-24 22:30 — GitHub → posts (user: "draft about my latest 2 repos"): gen/github_posts.py reads the public GitHub API (no key) — latest pushed repos (forks skipped) + newest commit subjects — and grounds one on-voice post per repo in those real facts; registered as the github_drafts chat tool (user configurable via arg or strategy file). Live run for MrFadiAi: openstanley diversity-engine post + kino-seedance-studio open-source post, both grounded in actual commits. 4 tests (443 total).
2026-08-24 22:50 — voice/account fix (user: "drafts should always follow the selected account's voice + language"): four newer draft paths (github posts, hook remix, trend_post, trend scout) called load_rubric() which DOES NOT EXIST — hasattr guard silently injected EMPTY voice, so those drafts ignored the account entirely. All four now call voice_prompt_block() (the account-scoped rubric+style injector the main draft path uses). TG: approval + mini cards now carry a [handle] header showing which account each draft rides, and /help states the account rule. /account <id> was already the switch.
2026-08-24 23:10 — DEEP TRAIN (user: "deep learning so the agent has a great brain of the selected account"): Agent.deep_train() = full immersion chain on the ACTIVE account — own posts paged to 800 + own replies to 400 ingested, metrics ground truth, style+voice rebuild, niche study with deep pull (cap raised 400→800) + hooks, then dedicated scan+learn reflections. Returns a brain report card (posts/replies ingested, rules, journal entries, hooks, seconds). POST /api/loops/deep-train + TG /train command (progress message + report when done) + /help entry. Read-only on X. 1 test (444 total).
2026-08-25 00:16 — /train route bug fixed: /api/loops/deep-train was shadowed by the /api/loops/{name} catch-all (registered earlier) so the user's first /train 404'd silently — moved to /api/deep-train, TG command updated. 444/444.
2026-08-25 00:55 — brain stub sections filled (user: "few sections are empty"): content-pillars, voice-cards, competitor-notes were placeholder stubs because reflect's LLM pass rarely proposed file_updates and the deterministic fallback only wrote 2 of 5 docs. The fallback now deterministically writes ALL FIVE from scan stats (pillars from weighted topics, voice cards from casing/punctuation registers incl. question/colon/ellipsis modes, competitor notes from niche engagement leaders). Both accounts' files regenerated live; verified real content. 3 new tests, 2 expectations updated (447 total).
2026-08-25 13:40 — short-draft fix (user: "why are drafts always so short?"): chat drafts averaged 69 chars against the account's real 167-char average because (a) the voice-lock post band floor was avg*0.4=67 chars, letting anything short pass, and (b) the prompt never showed the model the account's actual length distribution. Fix: band floor raised to avg*0.6 (acct2 post band now 100-280), and the style block now carries the real length mix (over240 35% · under80 26%) plus an explicit "WRITE TO THE AVERAGE, NOT BELOW IT". Both accounts' voice.md regenerated. 448 tests.
2026-08-25 14:30 — flat tool-args fix (user report: trend_post 'topic required' despite topic given): models emit actions in two shapes, nested {tool,args:{}} and flat {tool,topic:...}; parse_actions only read the nested one, so flat-shape calls arrived with empty args. Both shapes now parse — flat fields merge into args. 449 tests.
2026-08-25 17:55 — mini cards now quote the FULL draft (user: "I don't see the full draft at all"): the chat-born mini card truncated to 80 chars (SQL substr) while the loop cards quoted in full — the user was approving half-blind on every trend/chat/thread draft. Same quote contract everywhere: full text, 800-char safety ceiling only. 449 tests.
2026-08-25 21:10 — triple fix from log audit (user: "detect errors and fix") + internet access: (1) ENGAGE LOOP WAS DEAD — the gate's timestamp parser only understood ISO but posts store twikit's English format ('Fri Aug 21 00:20:34 +0000 2026'), so ALL 9 targets were rejected as 'age unknown' every 30 min for days; parser now handles both formats and live-verified: gate now judges real ages (all current targets correctly rejected as >24h stale — the wall working as designed). (2) faster-whisper reinstalled in the server venv — voice notes restored. (3) NEW web_read tool — the agent can now OPEN any URL and pull full readable page text (reader mode), not just search snippets; live-verified on HN; registered in the chat registry so 'read this article and draft from it' works. 452 tests.
2026-08-25 21:45 — reasoning upgrade (user: "both"): (1) THINKING MODE — z.ai's GLM-5.3 honors the Anthropic thinking parameter (live-probed: private scratch-space before the answer); chat() gained thinking_budget, temperature auto-unset with thinking per API contract. (2) DEEP RESEARCH — new deep_research tool: web_search → web_read top 3 pages (full text, not snippets) → one thinking-mode pass (2500 budget) over the whole material → grounded draft with sources recorded. Live: 31s end-to-end, 3 pages read, Arabic draft citing $285B software-stock wipeout, LegalZoom -20%, Publicis Sapient cutting 50% of SaaS licenses — all from the actual pages. 454 tests.
2026-08-25 22:58 — TG candidate runaway fixed (user: "it does not everything I asked" — one request made 9 drafts): (a) quote blocks that repeat an EXISTING draft's text are skipped — "show me the post" re-quotes it and re-saved duplicates; (b) 3+ consecutive SHORT quote lines are a THREAD — merged into one threaded draft instead of N 40-char fragments; (c) the 8 fragment drafts purged and the wrongly-scheduled #2484 reverted to a plain pending draft. 454 tests.
2026-08-25 23:20 — RTL guard, deterministic (user: "X does not support RTL with English in the beginning"): voice_lock now hard-penalizes (-30) any Arabic-dominant post (>20% Arabic chars) whose FIRST word is Latin script — X breaks rendering of RTL posts opening with English. Applies everywhere the lock runs (create, trend, github, chat, threads); pure-English posts untouched. Live-verified: EN-open scored 55 with the violation, AR-open clean. 456 tests.
2026-08-26 00:15 — post-type matrix completed with link-reply (user: "agent should be capable of thread, quote, reply, normal post, normal post with reply"): drafts carrying meta.link_reply now ship the URL as the FIRST REPLY under the published post — clean body, link still delivered, charged against the reply cap (a cap bounce skips only the link, never the post). GitHub drafts auto-carry the repo URL; schedule_draft tool gained a link param ("schedule this with the link in a reply"). Full matrix now: post, post+image, post+link-reply, thread, reply, quote, quote+image.
2026-08-26 01:15 — [show] button (user: "add another button show alongside approve/reject — when I click show I read the full draft"): every approval/mini/drafts card row is now [approve N][reject N][show]; show sends the COMPLETE draft as its own message (full body, every thread tweet numbered, link-reply noted) and is strictly read-only — the card is never rewritten or cleared. 459 tests.
2026-08-26 02:30 — hermes-grade upgrade (user: "solve the bugs, make the agent like hermes"): (1) x_trends transient 404 (code 34) — one in-function retry clears it (observed failing then succeeding 55s apart). (2) ENGAGE LIVE TARGETS — the loop now searches the account's themes FRESH before drafting replies (refresh_niche_targets upserts live posts, then the gate judges them) instead of rereading posts stored days ago, which were 37-45h stale every single pass. (3) AGENTIC TOOL LOOP — chat tool execution now chains up to 3 rounds (tool → results fed back → model may emit the next action → …) with exact-repeat dedupe; search → read → draft completes in ONE conversation turn like hermes. 459 tests.
2026-08-26 02:56 — ENGAGE LOOP LIVES (test-and-debug loop, user: "loop test and debug"): the live refresh stored +9 fresh posts but the gate still saw 37-47h-stale targets — the target picker ranked by ENGAGEMENT so days-old viral posts crowded out the fresh ones. Picker is now FRESH-FIRST (only posts inside the ~24h window reach the gate, then ranked by engagement). Live-verified on first run after fix: the loop drafted its first-ever 3 niche replies (Arabic, on-voice, approval-gated #2502-2504) with real gate judgments ("age 22.4h — cooling"). 459 tests.
2026-08-26 03:15 — reply drift fixed (user: "drafts are short and all end with a question"): the diversity engine + length directive only covered the post paths; the REPLY prompts had neither, and the "replies are worth 27-75x likes" heuristic made the model end every reply with engagement-bait questions. Both reply system prompts now carry the question ceiling (one in three, ask only when genuinely curious) and the write-to-the-owner's-average length rule. 459 tests.
2026-08-27 18:13 — rejection learning loop + instruction memory + chat watchdog shipped (UPGRADE_BRIEF_LEARNING.md). (1) REJECTION LEARNING: every reject path (web endpoint, TG command, TG one-tap, nightly sweep as reason=expired) now stamps meta.{rejected_reason,via,at}; >=3 pending owner rejections fire a background reflect('rejection') contrasting them against approved drafts -> DON'T-rules (source=rejection) + journal; the nightly 04:17 pass runs BEFORE the expiry sweep so real taste never gets diluted by hygiene (expired drafts are stamped, never learned); POST /api/rejection-learn forces a pass. (2) INSTRUCTION MEMORY: directive-shaped chat messages (EN+AR regex gate -> one small LLM confirm+normalize, zero cost on ordinary messages) become standing brain rules (source=directive) captured on web + TG chat, deduped at 0.6 token overlap, acked in-chat with the rule id; brain_context hoists directives into their own OWNER DIRECTIVES block AHEAD of learned rules with its own budget share; remember_rule chat tool persists explicit asks. (3) CHAT WATCHDOG (system/watchdog.py): chat LLM failures (3 consecutive -> TG alert + degraded flag, success re-arms), tool failure rate (>50% of last >=10 -> one alert/episode), chat-draft burst guard (7th chat-born save within 1h BLOCKED until the window drains — the runaway-candidate guard made general), TG handler streak (10 -> alert); GET /api/watchdog + watchdog line in TG /status; every note() is best-effort and never raises into the monitored path. 29 new hermetic tests (488 total); conftest gained per-test watchdog reset + default not-a-directive stub over the instructions LLM seam so no future chat test can leak a real call through it.
2026-08-27 18:35 — live server restarted onto the learning build (4b512e7) on the project .venv interpreter (was the hermes runtime python): /api/watchdog live, cookie mode intact, TG poller polling (task_alive), autopilot resumed (tick 199, next 18:50).
2026-08-27 18:55 — QA+improve LOOP established (hourly cron, session job c2a899ad): each pass = full hermetic suite + real-LLM harness eval + live agent_log/watchdog mining -> one verified fix, sandboxed (dryrun tests, harness makes zero X writes, publish stays human-gated). ITERATION 1 shipped (0645b57, 492 tests): live log mining caught the create loop dying with "Missing LLM API key" (old hermes-python server) which the restart fixed, and a FALSE-RED smoke: GLM always emits a thinking block before text, so the probe's 16-token cap hit stop_reason=max_tokens with zero text — a healthy LLM reported red every boot. Both LLM transports now raise with stop_reason + block shape instead of returning "" silently (chat()'s retry self-heals), probe cap 16->64. Live-verified: smoke all green, llm 'pong'. HARNESS BASELINE (real-LLM, active account brain): voice 63.4 / algorithm 70.0 / bilingual 77.8 / tools 100 / safety 85.7 / TOTAL 76.0 — voice is the weakest suite and next iteration's target. Backlog mined from logs: empty-error-string logging in study/engage search failures (str(e)=="" hides the cause).
2026-08-27 19:55 — QA loop ITERATION 2 shipped (0e43306, 499 tests). Mined: zero warn/error logs since the LLM fix (log clean). Target was the voice baseline 63.4: root-caused to style_prompt_block hiding three of the four knobs drafts are judged on — sentence p50 (6 words), question rate (0.12), the colon-led opener signature (68% of posts), and the account's own vocabulary. The block now carries all of them (colon line only when >=30%, the account's actual signature). Re-measure: style_distance 56.4->59.9 (deterministic closeness improved), total 62.9 = flat within n=5 noise — recorded honestly; eval measurement power is the next-but-one fix. Also shipped err_str() for the four X-search log sites whose exceptions str() to '' (empty-error-string backlog item). The loop's own verify pass caught my first cut KeyError'ing on minimal profiles (5 suites red) — fixed + regression test BEFORE shipping; final 499/499, server restarted, smoke green. NEW BACKLOG (evidence during rollout): boot-smoke identity 404 was TRANSIENT (fresh client 2/2 OK seconds later) but pins a false red — boot probe needs one delayed retry + a periodic re-smoke so stored state can't go stale; harness voice n=5 needs more samples for deltas to mean anything.
2026-08-27 22:05 — QA loop ITERATION 3 shipped (8ed8df2, 501 tests; two queued cron fires consolidated into one pass — a back-to-back identical sweep adds no coverage). System was clean (zero warn/error, watchdog idle, autopilot 0 errors), so the pass took the mined backlog: (1) BOOT-SMOKE FALSE-RED killed — identity probe retries once after 15s (timeout 15->45; boot smoke is backgrounded so startup never blocks) + hourly re-smoke job at :11 keeps the stored report fresh, so a transient X 404 can never pin red for an hour again (2) HARNESS MEASUREMENT POWER — this install's sample_count 5->12; at n=5 identical configs swung +-5 on voice. Honest n=12 verdict on the fingerprint prompt: 62.2 — NEUTRAL on score; drafts visibly obey it (colon-led openers throughout every sample) but (a) voice_match has a ~70 structural ceiling (60 base + 10 length; nothing earns more), (b) style_distance vocab-overlap punishes Gulf-dialect words absent from top_terms (mined terms are MSA/tech nouns), (c) colon-led openers make one long 'sentence' by the splitter's rules. All three recorded as ITERATION-4 targets — fixing the METRIC where it mis-measures the real voice, not gaming it.
2026-08-27 23:10 — QA loop ITERATION 4 shipped (7b0d977). VERIFIED LIVE: the hourly :11 re-smoke fired green at 22:11 (iteration 3's fix working). HEADLINE: the ground-truth calibration experiment — scoring the account's REAL posts on the voice suite's own math gave 61.7 vs drafts' 62.2, proving the absolute scale is compressed/dialect-blind and '62' never meant '62% good'. voice_eval now anchors on a real_post_baseline (>=5 own posts on the same metrics): score = drafts/baseline capped at 100 with an EXCEED note; fresh accounts keep the absolute scale with a note. 100 now means 'indistinguishable from the owner's own writing' — the actual product goal. 5 hermetic tests (own_posts patched at the seam — the shared test DB's active account is nondeterministic). INCIDENT, FULL OWNERSHIP: while debugging the calibration tests, an ad-hoc python shell ran WITHOUT OPENSTANLEY_TEST_DB and executed DELETE FROM posts WHERE is_own=1 against the REAL DB — both accounts' ingested own-post history was wiped (niche posts + 2494 metric snapshots + style profiles survived; nothing was ever posted to X — the core sandbox promise held). Recovered same-hour: junk rows removed, active account restored to 2, both timelines re-paged from X read-only — account 1 fully back (20 posts); account 2 at 100 when X 429'd the deep paging (patient top-up scheduled + the nightly study deep-pull self-heals it: <150 stored triggers the pull to 800). Lesson recorded in memory: debug repros belong in test files under monkeypatch, never in bare shells. Eval run 802's baseline is fixture-contaminated and meaningless; next iteration's calibrated eval runs on the real restored corpus.
2026-08-27 23:45 — QA loop ITERATION 5 shipped (bdcc008, 511 tests). Mining: 23:11 re-smoke red ONLY on the expected timeline 429 (rate window from the recovery paging — self-heals), zero loop errors. TARGET: tonight's three separate transient X-read failures (smoke identity 404 code-34, recovery me() 404, timeline 429s) — the auth-heal decorator catches auth failures, nothing caught flakiness, so every read loop (study deep-pull, engage refresh, mentions) died on it. _read_retry now stacks OUTSIDE _auto_heal on me/user_tweets/user_replies/search/mentions: exactly ONE retry (60s on 429, 15s on code-34 404, logged); a 404 WITHOUT code 34 is permanent (deleted tweet) and never retried; writes are NEVER re-fired (duplicate-post risk — read methods only). 5 tests pin the classification + one-retry semantics. One unreproduced e2e flake observed (create drafted 0 once, green in 3 subsequent runs incl. exact ordering) — WATCH ITEM: if it recurs, root-cause the create-chain idea/voice-gate interaction. Account-2 corpus still at 100 awaiting the nightly study deep-pull (<150 triggers the 800-pull); rate window should be long reset by then.
2026-08-28 00:45 — QA loop ITERATION 6 shipped (af35948, 512 tests). MAJOR FINDING: the study deep pull has been a SILENT NO-OP — the cookie client pages via cursor WITHIN one user_tweets call, but the loop chunked it into repeated limit-100 calls that each restarted at page 1; upsert deduped the repeats to zero while the log claimed success ('+400 posts (had 104)' day after day with the count never moving; tonight '+800' stored nothing). New accounts were capped at ~100 known posts the whole time — the 2026-08-21 'learned only 30' fix never actually delivered. Fix: ONE call with the full 800 limit (deep_train's proven pattern), dead generator removed, and the log now reports NET NEW stored rows ('had N, fetched F') so a fetch-counting lie can never hide this again. test_deep_pull's fake rewritten to model the REAL client pagination + a skip-when-known test. The recurring harness determinism flake root-caused by the same class of shared-state leak: scan_account (tools suite) rewrites style_profile between the compared runs — the check now compares two consecutive algorithm-only runs. Live restore of acct2's corpus blocked twice by X 429s tonight (drained by the study searches preceding the pull); a delayed study retry is scheduled and the nightly study will land it under the fixed code regardless. First calibrated voice eval deferred until the corpus is whole + a fresh scan.
2026-08-28 01:20 — QA loop ITERATION 7 (measurement pass, no code change; suite green 512/512 — suite_i7 tail verified). System clean all night (zero warn/error, smoke 7/7, watchdog idle). Fresh scan over the RESTORED corpus (213 posts, deeper history): the 100-post artifact's '100% English' flipped to the truth (58% AR / 19% EN / 22% mixed), voice rebuilt, brain +3 rules +4 strategies. FIRST CALIBRATED EVAL on the whole corpus: voice 100.0 — drafts EXCEED the real-post baseline by 6.4 (baseline 61.0 over 25 real posts, drafts 64.9) — by the product's own definition the drafts are now indistinguishable-from-or-better-than the owner's writing on voice; algorithm 72.7, bilingual 72.2 (n=12). Voice work is DONE by its own metric; algorithm factors are the next improvement target if pursued.
2026-08-28 02:50 — QA loop ITERATION 8 shipped (8444017, 520 tests). System clean (zero errors, autopilot tick 206, corpus at ceiling 778, 10 drafts awaiting owner). Target: the algorithm suite's 72.7 — factor mining showed Language quality negative on 67% of drafts and Topic affinity on 75%. ROOT CAUSE (metric mis-measuring the real voice, same disease as the voice suite): a SINGLE tatweel is the standard Arabic definite article before Latin loanwords ('بالـ API') — 7/50 of the owner's own posts use it, zero use runs — but the rule fired on any occurrence, punishing the account's actual Gulf-Arabic style on 8/12 drafts. Now fires on RUNS (2+) only; language quality flipped -0.8/67% -> +1.7/8%, suite 72.7 -> 75.1 (n=12). Also cleaned _account_topics ('https' and the account handle were literally in the live topic list). Known remaining: topic affinity's dialect gap (drafts use Gulf dialect, mined topics are MSA/tech nouns) — needs semantic matching, diminishing returns, left documented. Note: TinyFish key not yet pasted — search still rides DDG until then; first keyed search's live verification is queued for a future pass.
2026-08-28 03:05 — TinyFish LIVE: owner's key pasted via Settings API (masked sk-tin…), both endpoints verified against the real API. Found+fixed immediately by the live check: the fetch endpoint is the API ROOT (POST api.fetch.tinyfish.ai), not /fetch (which serves the marketing 404 — the unauthenticated probe couldn't distinguish this). Now live: web_search returns ranked TinyFish results ($0), web_read returns browser-rendered markdown (via=tinyfish) including JS pages the old reader could never read; DDG stays the automatic fallback. 520/520.
2026-08-28 03:45 — QA loop ITERATION 9 shipped (aef825a, 521 tests). System clean all pass (zero errors, smoke green). Target: the safety suite's 85.7 — mining showed caps_enforce FAILED 'CAP NOT ENFORCED'. ROOT CAUSE: the check wrote the PRE-v0.5 global safety_counters key while counters have been per-account (safety_counters:<id>) since v0.5.0 — on the real account it tested the wrong storage and reported a phantom failure while the cap itself worked. Fixed to use safety._key(None) (can never drift from storage again) + regression test. Verified on the live account: safety 100.0, 'SafetyCapExceeded raised at cap'. OBSERVED FOR ITERATION 10 (test-isolation audit): one opening run failed 7 state-heavy tests (digest/insights/harness determinism) that all passed unchanged in the next run — cross-run residue in the shared test DB; plus a 1-in-5 deep_pull flake (solo-green, no repro). Both are shared-DB state dependencies in older test files, not product bugs.
2026-08-28 04:10 — QA loop ITERATION 10 shipped (conftest pin, 521 tests): the test-isolation audit found the flake cluster's ROOT CAUSE in minutes — the ACTIVE ACCOUNT is shared DB state persisting across suite runs; insights/digest seeds hardcode account 1 while product queries read the active account, and the test DB was literally sitting at active=2 from a previous run's residue. One autouse conftest fixture pins account 1 around every test (switching tests unaffected — reset happens between tests). Proof: the 34-test flaky cluster passes against incoming active=2, and the full suite runs 521/521 from that hostile state. This kills the whole cross-run flake class (digest/insights/harness/deep_pull), making every future 'green' unqualified.
2026-08-28 04:35 — QA loop ITERATION 11 (measurement pass, no code change; suite green). Re-measured bilingual after iteration 8's tatweel fix: 72.2 -> 97.2 (11 of 12 cases now 3/3; the one 2/3 is a mixed-request draft detected as pure-Arabic — borderline judgment, not a defect). The stored number had simply never been refreshed. FULL HARNESS PICTURE now: voice 100 (calibrated) / bilingual 97.2 / algorithm 75.1 / tools 100 / safety 100. System error-free for ~7 hours. Nothing broken found this pass — per the loop's discipline that is a VERIFIED CLEAN pass, not a reason to invent work. Remaining known limitation (documented, diminishing returns): algorithm topic-affinity dialect gap.
2026-08-28 04:50 — QA loop ITERATION 11 correction + fix: the pass's suite was '520 passed, 1 ERROR' (a Windows os.replace lock race on brain/rules.md during test setup — AV/indexer holding the destination), initially mislogged as green; corrected here, never amended. Fixed at the source: _atomic_write retries the rename up to 5x — the same race could drop LIVE brain writes on Windows. 522/522, zero failures and zero errors, server restarted on the fix.
2026-08-28 06:20 — QA loop ITERATION 12 (verified-clean measurement pass; no code change). Mining: the 04:17 nightly job ran the full chain in production for the first time (metrics 20 posts captured, brain reflect +1 rule, expiry sweep); zero warn/error all pass; queue discipline working as designed (create skips at 13 pending — 17 drafts await owner taps; yesterday's 9 expire 08-30 untapped, by design). Suite 522/522, zero failures AND zero errors. Full 5-suite post-fix snapshot stored in harness history: voice 100 / algorithm 74.2 / bilingual 97.2 / tools 100 / safety 100 -> TOTAL 93.1 (algorithm's n=12 swing 74.2-75.1 is sample variance around the documented dialect limitation). Nothing broken found — clean pass, no invented work.
2026-08-28 07:40 — QA loop ITERATION 13 (verified-clean pass, no code change; suite 522/522, zero failures/errors — 6th consecutive fully-green full-suite run, which closes the e2e-create watch item from iteration 5: never recurred since the active-account pin). Morning mining shows the whole organism healthy: 07:00 trend-scout drafted #2631 from a live niche topic while create throttles at 20 pending; the learn phase captured 60 posts, rebuilt the voice rubric (ar/en/mixed), and the brain PRUNED (2 rules retired across two reflections — active revision, not just accumulation); engage live-refreshed +4 targets and the 24h wall correctly rejected the cooling ones; watchdog clean. Note for the owner: all 98 rejected drafts are sweep-expiries — zero owner reject decisions recorded, so the rejection-learning feature has never had input; it activates the moment you tap reject on any draft card.
2026-08-28 08:45 — QA loop ITERATION 14 (verified-clean pass; suite 522/522, 7th consecutive, zero failures/errors). The owner started tapping this morning: 11 drafts approved+scheduled (first publishes since the loop began incoming at the 09:00 slot — through the human gate, exactly the product's design; the QA loop's own zero-post contract applies to the loop's actions, never to the owner's approvals). Study pulled +58 niche posts, hooks bank at 20 patterns, engage wall rejecting stale targets correctly, smoke green, zero errors. Next pass's special check: the 09:00 publish event — safety caps, slot spreading, and media attach all firing live for the first time under the loop's watch.
2026-08-28 09:50 — QA loop ITERATION 15 shipped (756ca23, 525 tests): found+fixed the STRANDED-APPROVALS bug. The 11 'approved' drafts are @orbexai (account 1) cards approved ~a week ago that became invisible when account 2 went active — the publish loop serves the ACTIVE account's client, and every draft read/write filters by it. They sat scheduled-but-unshippable ~3 days with zero signal; 3 were 'due' today. CORRECTION of iteration 14's reading: the owner did NOT tap this morning — nothing changed; I had never counted status=approved before (recorded, never amended). Fixes: publish loop alerts (log + one TG message per stranded draft, '/account N to publish or reject'), TG approve/reject explain cross-account drafts instead of the misleading 'No draft #N'. Never auto-posts cross-account. Also made the TG chat-candidate tests self-isolating (dedupe reads the last 60 drafts globally — one failed run's residue blanketed later runs; caught by stashing: failed on pre-edit code too). The stranded alert will fire on the next publish cycle (~10 min) and tell the owner their options.
2026-08-28 10:20 — QA loop ITERATION 16 (verification pass; suite 525/525, zero failures/errors): the stranded-approvals alert from iteration 15 FIRED LIVE at 09:50 — publish cycle detected #2386 due-and-stranded, logged the warn, ran the TG alert path to completion (stranded_alerted=[2386], written only after notify fires; one alert per draft by design — #2387 alerts when due at 13:00). The owner's move remains: /account 1 to ship the 11, or reject them. System otherwise clean.
2026-08-28 11:45 — TG WEDGE (owner report 'agent not responding'): the 11:26 'draft about my repo' message was received, then the handler hung silently for 6+ min — no reply, no error, no timeout (a slow-dripping LLM stream resets httpx's per-read timeout indefinitely). Cleared by server restart; hardened in 5a3b392 (526 tests): every TG handler now caps at 300s wall-clock (slot frees, error logs, owner gets 'ran too long, please resend') + a typing indicator so legit multi-minute tool chains look alive. ALSO: the owner's 11:26 taps were the FIRST owner rejections ever recorded — the rejection-learning loop finally has real input.
2026-08-28 12:15 — QA loop ITERATION 17 shipped (c1f3130, 9f45678, R-id fix; 529 tests): the first live rejection-learning pass over-learned — 18 rules from 8 taps, the same 3 lessons paraphrased 5+ times. Built the consolidation ladder: token-overlap pass (<8 rules) + one validated LLM clustering call (>=8): every id must be accounted for exactly once or the proposal rejects whole. Debugging live runs found two of my own bugs — token overlap caught 0 of the real (paraphrase) dupes, and the validator rejected the model's CORRECT proposal because it echoes 'R72'-style ids — both fixed. LIVE RESULT: 18 rules -> 3 canonical: (1) no صدك-agreement templates — creator-centered specific praise, (2) no meta-commentary/scolding/warnings about Twitter culture, (3) replies must name specifics and commit to action. Your taste, distilled. Also fixed two test-layer leaks found by the full suite (rejection-test isolation from other suites' stamped rejections; conftest reset of the TG module's process-level rate-limiter state that silently dropped later tests' sends).
2026-08-28 13:15 — QA loop ITERATION 18 (verified-clean pass; suite 529/529, zero failures/errors). System quiet and healthy: zero errors since 12:15, no TG activity (owner's repo message not yet resent), queue steady at 19 pending + 11 stranded-approved. No new drafts since the consolidation — engage's 24h wall correctly found no fresh-enough targets on the quiet account, and create is throttled; the 3 canonical rejection rules will visibly shape the next naturally-drafted replies. Clean pass, no invented work.
2026-08-28 14:35 — QA loop ITERATION 19 shipped (530 tests): found+fixed another silent swallow — at 13:30 the engage gate passed 4 targets but 0 replies landed with ZERO logs (not voice-lock, not error): the 'if not reply: continue' ate the failure mode. Now accepts 'text' as a fallback json key (model variance) and logs author + actual json keys when empty. Next engage pass will either draft or SAY why not. System otherwise clean (zero errors, queue steady).
2026-08-28 14:50 — QA loop ITERATION 20 (verification pass; suite 530/530, zero failures/errors). Both of yesterday's fixes verified LIVE in the first post-deploy engage passes: (1) 14:25 DRAFTED a reply whose content shows the 3 consolidated rejection rules working — opens with specific praise, leads a concrete technical point (compiler output -> gas), no صدك template, no meta-commentary (draft #2647); (2) 14:30 gate kept 0 and SAID so honestly — the no-silent-swallow contract holds end-to-end. Zero errors. The learning loop is closed: owner taps -> 3 rules -> visibly better replies. Clean pass.
2026-08-28 15:45 — QA loop ITERATION 21 (verified-clean pass; suite 530/530, zero failures/errors). One false alarm during mining (gate line with no follow-up = my read raced the in-flight draft; the 'drafted 1' line + reply #2648 landed seconds later — lesson: mine AFTER the pass completes, not mid-draft). Reply #2648 again rule-conformant: engages the parent's specifics (4 apps / 105 days / 5.4M), personal commitment, Gulf dialect. Zero errors, queue healthy. Clean pass.
2026-08-28 16:10 — TG 'still down' report #2 root-caused and fixed: the chat history held the agent's own confession ('my last few replies glitched out empty') — an all-thinking STREAM closing with zero text deltas stored a blank assistant reply and sent nothing. The non-streaming empty guard from QA iteration 1 never reached the stream transports; both now raise with stop_reason, the TG path shows a visible error + watchdog count instead of silence. Also confirmed the owner's 15:43 resend DID work end-to-end (github_drafts ok, draft #2649 on-voice, user interacted) — the report covered the earlier blank attempts. 532/532, deployed.
2026-08-28 16:35 — 'still no draft' report: the 15:53 chain had researched (web_read ok) and ended its turn WITHOUT emitting a post — a model-behavior gap, not an infra failure. Delivered the missing drafts directly (github tool: #2650 openstanley, #2651 kino-seedance-studio — the exact repo asked for — TG cards pushed). Shipped the GUARANTEED-DRAFT NUDGE (534 tests): a draft-intent message that yields no candidates gets ONE forced write-the-post-now turn, so an explicit ask can never again walk away empty; informational asks never trigger it. Debugging note: heredoc-written regex lost its backslashes twice (\b → backspace) — rebuilt chr(92)-safe, a lesson already familiar from the stranded-alert newline.
2026-08-28 16:50 — ITERATION 22: the user rejected the delivered drafts #2650/#2651 at 16:12 (rejection learner absorbed 3 more, +1 rule) and re-sent the repo request — that turn became a PHANTOM: inbound logged, then no assistant message, no draft, no error, no 300s-timeout firing, and py-spy shows no handler thread (only the getUpdates long-poll). Evidence eliminated: event-loop freeze (engage ran), voice-lock, empty-stream (would raise now), send_stream abort (would warn). Deployed chat-turn START/END instrumentation to bracket the next occurrence; server healthy otherwise, suite 534/534. Next TG message will either work under the nudge+guards or pinpoint exactly where it vanishes.
2026-08-28 17:15 — 'still not doing it right way': the 16:12 phantom turn was still unanswered AND the full stored request revealed WHY the earlier drafts were rejected — the learner's R73 (born 16:13 from those very rejections): no own-repo changelogs; frame around what it does for the user. The owner's real posts (Linea checker announce style: casual, personal, link included) set the template. Hand-wrote two kino-seedance-studio drafts the RIGHT way — personal-use framing, Gulf Arabic (#2655) + the 'Finally' English pattern (#2656), link in first reply — cards pushed. The phantom-turn cause remains bracketed by instrumentation awaiting the next TG message.
2026-08-28 18:15 — QA loop ITERATION 23 (verification pass; suite 534/534, zero failures/errors): the instrumentation captured its first FULL HEALTHY TURN — the owner's 17:21 'Show me' ran START → list_drafts ok → END (276-char reply, nudge correctly silent for an informational ask). The chat pipeline is confirmed working end-to-end on the current build; the 16:12 phantom did not recur. kino drafts #2655/#2656 pending the owner's taps. Zero errors. Clean pass.
2026-08-28 18:35 — 'still responds to something else': read the actual TG session — the agent was NARRATING ('On it', 'Pulled 1 fresh draft, ID 2660') instead of SHOWING the post. Then tested the owner's exact message through the real path myself and hit the taproot: 'empty stream reply (stop_reason=max_tokens)' — GLM's thinking phase eats the whole 1200-token budget before writing any text. Same message at 4000 budget delivered EXACTLY the ask: kino post inline in the owner's voice + link + scheduled. All three chat builders raised 1200->4000 (b408d67, 535 tests, deployed, live-verified again post-deploy). This one number was the real villain behind the entire 'agent is gone' day: starving thinking-mode turned replies blank, phantom, or process-chatter. Side effect of the verification run: draft #2661 (kino post + link, scheduled 8pm) was created by the schedule_draft action — the owner's actual ask, awaiting their eye.
2026-08-28 19:10 — QA loop ITERATION 24 (verified-clean pass; suite 535/535, zero failures/errors). Session tail confirms the budget fix working in the stored turns: full 800+ char deliverable replies (the kino post shown inline + link handling explained), and the second repeat correctly deduped ('your messages seem to be repeating'). #2661 (kino + link) approved and scheduled 20:00 tonight — the owner's original ask, shipping through the normal gate. Queue 37 pending / 12 approved. Zero errors since deploy. Clean pass.
2026-08-28 19:55 — DIALECT LEARNING shipped (owner request: 'the perfect Arabic dialect, dependent on the X account — study it'): gen/dialect.py mines the account's own posts (family markers + char words + verbatim examples), one validated LLM pass extracts the account's real constructions (never invented — grounded in samples only), and dialect_block() now rides voice_prompt_block into EVERY non-English draft. LIVE on account 2: IRAQI at 0.89 confidence (اني/هسه/اكو/هيج/جنت/مو/راح/علمودها from their own posts; avoid-list names Levantine هيك, Gulf الحين, Egyptian دلوقتي — exactly the wrong mixes that made sentences 'not so logical'). End-to-end draft test: clean Iraqi markers, zero wrong-dialect leaks. 6 tests, 541/541, deployed. The profile refreshes on every scan, so it follows the account as its dialect evolves.
2026-08-28 20:05 — QA loop ITERATION 25 (verified-clean pass; suite 541/541, zero failures/errors). Quiet since 19:11 — hourly smoke green, zero errors. The agent's two kino thread drafts (#2666 4-tweet, #2667 3-tweet — both opening in the owner's Iraqi voice from the new dialect system) await their taps. The 18:38 owner-driven reset (calendar + queue cleared) left the system lean: 2 pending / 11 stranded-approved. Clean pass.
2026-08-28 21:15 — QA loop ITERATION 26 (verified-clean pass; suite 541/541, zero failures/errors). The rejection learner absorbed 13 more owner rejections across three passes (+consolidation ran; one transient LLM read-timeout caught and logged by the fallback). #2667 rejected, #2666 (the 4-tweet kino thread) still pending. Engage drafted 3 fresh niche replies under the dialect+rules regime. Zero errors otherwise. Clean pass.
2026-08-28 22:20 — HERMES-GRADE AGENT DISCIPLINE shipped (owner: 'the agent does not seem like an agent; research how to improve it and make it respond like hermes'): studied the actual Hermes agent source installed on this machine (its TOOL_USE_ENFORCEMENT / TASK_COMPLETION / parallel-call guidance — GLM is on its enforcement list) + agent-design literature (inflectra prompt-engineering guide, towardsai 2026 patterns). The gap: OpenStanley's prompt never forbade promise-without-action. Now AGENT_DISCIPLINE rides both system prompts: act or deliver in the SAME message, full post text in a quote block (never a bare draft id), batch independent tool calls, never fabricate. Live test on a fresh ask: opens 'Here's the draft', full Iraqi-dialect post delivered, two-line rationale — deliverable-first, Hermes-shaped. 541/541, deployed.
2026-08-28 21:45 — 'it does not respond, fix it': root cause = the TOOL FOLLOW-UP prompt asked for a REPORT ('1-3 short sentences reporting what happened'). The owner's 'not long enough, add more details' turn ran web_read correctly, then replied with the research findings — never the post. _followup now takes the original request: draft-intent asks get DELIVERABLE MODE ('the quote block IS the reply — no preamble, no report', 4000-token budget) grounded in the tool results; informational asks keep the terse report. Live replay of the exact failing message returns a full detail-rich Iraqi-dialect post. This was the last gap in the discipline chain: first-turn delivery (AGENT_DISCIPLINE), guaranteed nudge, and now post-research delivery. 541/541, deployed.
2026-08-28 22:15 — QA loop ITERATION 27 (verification pass; suite 541/541, zero failures/errors): the session history holds the deliverable-mode fix working on the owner's own retry — 'Not long enough, add more details' at 21:30 got a 1092-char reply ('Here's the longer version — every line pulled from the actual repo:' + full quote-block post) versus the 338-char research-report the same words received pre-fix. The discipline chain is confirmed end-to-end in the wild: first-turn delivery, post-research delivery, guaranteed nudge. Zero errors. Clean pass.
2026-08-28 23:10 — QA loop ITERATION 28 (verified-clean pass; suite 541/541, zero failures/errors): the full-discipline pipeline running as designed on the owner's 22:15 repo request — schedule_draft (#2687 for tomorrow 20:00) + web_read research + 4-tweet thread (#2688) + a 1719-char deliverable reply, all in ONE turn. Zero errors. Queue: 1 pending / 12 approved. Clean pass.
2026-08-29 00:05 — QA loop ITERATION 29 (verified-clean pass; suite 541/541, zero failures/errors): overnight rhythm intact — the learn loop ran (60 posts, voice rebuilt, +1 rule / 3 strategies), engage's 24h wall correctly held (0 of 8 stale targets), the agent answered an informational ask cleanly (353-char reply, list_drafts, no spurious nudge). Zero errors. Clean pass.
2026-08-29 01:05 — QA loop ITERATION 30 (verified-clean pass; suite 541/541, zero failures/errors). Overnight quiet: engage correctly passed on cooling targets, smoke green on the hourly beat, zero errors. The milestone pass — 30 iterations, 541 tests (from 488), ten silent-failure classes killed, agent rebuilt (discipline + dialect + delivery), zero loop-caused posts. Clean.
2026-08-29 02:05 — QA loop ITERATION 31 (verified-clean pass; suite 541/541, zero failures/errors): deep-night quiet, engage's age wall correctly rejecting all cooling targets, zero errors. Clean pass.
2026-08-29 03:05 — VOICE POISONING fixed (owner: 'drafts are so bad, voice not like my account'): the profile's examples — the exact texts the model imitates — were RETWEETS (RT @GoogleDeepMind, RT @BybitArabic...), because _top_examples ranks by engagement rate and amplified big-account content dominates. Every draft inherited corporate-announcement DNA. _own_writing() now filters RTs/bare mentions from examples AND rubric corpus; voice rebuilt live (force). New examples are genuinely the owner's (announcement style, short lines, links, Iraqi+crypto mix, ':]' smiley); test draft came out in his real format — 'جربته و النتيجة صدك تجنن', 'رابط المشروع اول تعليق'. 541/541, deployed.
2026-08-29 04:05 — TOTAL DASH BAN (owner directive 'never include dashes'): the em/en scrub survived but list-bullet hyphens and hyphenated compounds did not. Now: leading dashes drop (owner's lists run bare), inline hyphens become spaces, URLs exempt (stashed during scrub). Doubly enforced: brain rule R97 (source=directive, rides the OWNER DIRECTIVES block into every prompt) + the hard scrub at every save boundary. 542/542, deployed.
2026-08-29 02:50 — 'agent does not complete the response': the 02:18 turn diagnosed a real double-post problem (duplicate kino drafts #2687 20:00 / #2693 21:00) and stopped at the diagnosis — no fix, no question. Fixed the data live (#2693 rejected as duplicate; 20:00 stands) and added CLOSE-THE-LOOP to AGENT_DISCIPLINE: every investigation ends with the disposition in the same response — safe fix applied, or exactly one clear question for the owner. 542/542, deployed.
2026-08-29 03:15 — delete_draft capability shipped (owner: 'it still could not delete any draft'): the tool registry could list/schedule/regenerate but not REMOVE — the agent honestly said so ('I still have no delete action'). delete_draft {draft_id | delete_all_pending} now rejects (never hard-deletes), feeds the rejection learner, and is documented in the tools prompt so the model reaches for it on any delete/remove/clear ask. Live-verified; 544/544, deployed. The agent's capability set is now symmetric: create, list, edit (regenerate), schedule, remember, and delete.
2026-08-29 03:30 — QA loop ITERATION 32 (verified-clean pass; suite 544/544, zero failures/errors). The 02:39 boot smoke was a transient red (identity timeout during restart); a direct X probe confirms the wiring is healthy (@Mr_CryptoYT, 43696 followers) — the hourly :11 re-smoke flips the stored state green, exactly the false-red scenario it exists for. No TG activity since the delete-capability deploy (the owner has not tested it yet). Pending queue at 8. Clean pass.
2026-08-29 04:30 — QA loop ITERATION 33 (verified-clean pass; suite 544/544, zero failures/errors): the hourly re-smoke flipped the boot transient green (7/7) exactly as designed; deep-night quiet, zero errors, no TG activity. Clean pass.
2026-08-29 05:15 — QA loop ITERATION 34 (verified-clean pass; suite 544/544): THE LAST UNTESTED PATH IS CLOSED — the publish loop shipped the first post under all the new hardening at 04:30 (#2701, an engage-niche reply the owner approved from TG at 03:01, x_id=2093526627997147381). Full chain live: owner tap → slot → publish → X. The nightly 04:17 chain also ran complete (metrics 20 posts, brain +1 rule, rejection reflection). Zero errors. To be explicit: this was the OWNER'S approval shipping — the QA loop itself still has caused zero posts.
2026-08-29 06:15 — QA loop ITERATION 35 (verified-clean pass; suite 544/544, zero failures/errors): morning rhythm — engage live-refreshed +10 fresh targets, drafted 3 more approval-gated replies under the dialect+rules regime, zero errors. Clean pass.
2026-08-29 07:15 — QA loop ITERATION 36 (verified-clean pass; suite 544/544, zero failures/errors): steady engage rhythm (3 more approval-gated replies drafted, the age wall correctly rejecting cooling targets), zero errors. Clean pass.
2026-08-29 08:15 — QA loop ITERATION 37 (verified-clean pass; suite 544/544, zero failures/errors): 07:00 create ran under the throttled queue (17 pending), engage's age wall held, zero errors. Clean pass.
2026-08-29 09:15 — QA loop ITERATION 38 (verified-clean pass; suite 544/544, zero failures/errors): steady state — engage refreshing and gating correctly, zero errors. Clean pass.
2026-08-29 10:15 — QA loop ITERATION 39 (verified-clean pass; suite 544/544, zero failures/errors): engage gate passed 8/9 fresh targets (its best morning yet), autopilot cycling create, zero errors. Clean pass.
2026-08-29 11:15 — QA loop ITERATION 40 (verified-clean pass; suite 544/544, zero failures/errors): engage productive (8/9 targets passing, 3 more replies drafted), zero errors. 40 iterations: 544 tests, 10+ failure classes killed, full live-chain verified, zero loop-caused posts. Clean pass.
2026-08-29 12:15 — QA loop ITERATION 41 (verified-clean pass; suite 544/544, zero failures/errors): midday steady state, engage gating correctly, zero errors. Clean pass.
2026-08-29 13:15 — QA loop ITERATION 42 (verified-clean pass; suite 544/544, zero failures/errors): steady midday, engage gating correctly, zero errors. Clean pass.
2026-08-29 14:15 — QA loop ITERATION 43 (verified-clean pass; suite 544/544, zero failures/errors): autopilot create phase productive (trend-scout found 21 fresh posts, drafted #2741 from a live topic), engage gating correctly, zero errors. Clean pass.
2026-08-29 15:15 — QA loop ITERATION 44 (verified-clean pass; suite 544/544, zero failures/errors): THE AGENT USED ITS NEW CAPABILITIES END-TO-END IN ONE TURN — the owner's 14:28 request ran delete_draft (retry after a wrong-id miss, then ok), schedule_draft (#2749 at 20:00), and delivered a 4-tweet thread (#2750) with a 1463-char reply. The full agent stack working as designed: delete + schedule + deliver in a single turn. Zero errors. Clean pass.
2026-08-29 16:15 — QA loop ITERATION 45 (verified-clean pass; suite 544/544, zero failures/errors): quiet afternoon, zero errors. Clean pass.
2026-08-29 17:15 — QA loop ITERATION 46 (verified-clean pass; suite 544/544, zero failures/errors): another full agent turn — schedule_draft #2759 (20:00) + 4-tweet thread #2760 delivered + the owner approved it to a smart slot (2026-08-30 13:00, metrics-aware spread). Zero errors. Clean pass.
2026-08-29 18:15 — QA loop ITERATION 47 (verified-clean pass; suite 544/544, zero failures/errors): the publish pipeline working at capacity — #2755 shipped on schedule (owner-approved), and when the daily 4-post cap hit, #2753 was cleanly rescheduled to tomorrow 21:00 (the cap-bounce spread working as designed, not an error). Zero true errors. Clean pass.
2026-08-29 19:15 — QA loop ITERATION 48 (verified-clean pass; suite 544/544): cap-bounces spreading surplus approvals across days (#2756 → 08-31 13:00, #2762 → 08-31 09:00) exactly as designed; both kino posts (#2749, #2759) hold their 20:00 slots for tonight. Zero true errors. Clean pass.
2026-08-29 20:05 — QUEUE VISIBILITY shipped from reading the owner's last 20 agent messages: (1) 'where is the seedance draft' had got 'posted, deleted, or never saved' — the agent searched PENDING only while the draft sat APPROVED; (2) seeing today's schedule took FIVE turns of partial pulls; (3) 120-char previews forced per-draft refetches. Now: list_drafts query searches ALL statuses (text+source+repo, 280-char previews, prompt rule 'never claim missing without searching every status'), get_schedule returns the whole calendar in ONE call (time-ordered, today-first). Verified live: 'seedance' query instantly finds #2749 APPROVED for 20:00. 547/547, deployed.
2026-08-29 20:15 — QA loop ITERATION 49 (verified-clean pass; suite 547/547): cap-bounce spreading continues correctly (#2774 → Sep 1); both kino posts hold their 20:00 slots. Zero true errors. Clean pass.
2026-08-29 22:45 — X-186 SILENT KILL fixed (both kino posts died at 20:00 with 'tweet too long', nobody told): publish alerts on 186 with the fix path, agent discipline now enforces <=280 single / thread-if-longer, and both failed kino posts were re-split into proper threads live (#2749 3 tweets 21:07, #2759 2 tweets 21:08 — check X, they should have shipped). Test-side: my 186 test's isolation lambda created a fresh iter per call → probe returned forever → publish() hung the suite 8+ min; fixed with a shared iterator. 548/548.
2026-08-29 23:15 — QA loop ITERATION 51 (verified-clean pass; suite 548/548, zero failures/errors): kino #2749 confirmed PUBLISHED (21:10) on X; #2759 still approved in queue; boot smoke green 7/7; zero errors. Clean pass.
2026-08-30 00:15 — QA loop ITERATION 52 (verified-clean pass; suite 548/548, zero failures/errors): engage productive late night (3 more replies drafted), zero errors. Clean pass.
2026-08-30 01:15 — QA loop ITERATION 53 (verified-clean pass; suite 548/548, zero failures/errors): deep-night quiet, zero errors. Clean pass.
2026-08-30 02:15 — LINK CAP SACRIFICE fixed (owner: 'the kino post did well but had no repo link, why'): the link WAS queued as the post's first reply but the daily reply cap was exhausted at publish time (21:10) and the design silently skipped the link to save the post. Link replies are now EXEMPT from the engagement cap (they count in usage, never gated — part of the owner's approved post), and any future skip alerts the owner with the link. For the live post: the link can be sent manually as a reply under x_id 2093778280599949491, or ask the agent on TG. 549/549, deployed.
2026-08-30 02:50 — FULL APP SURFACE shipped (owner: 'the agent must have all capability of the openstanley app, it can do everything I ask, both app and Telegram'): 8 new tools (25 total) — app_status (one-call status with the schedule inside), approve_draft, reschedule_draft, edit_draft, run_loop (any loop on demand), list_ideas, brain_read, switch_account — plus a ROUTING table mapping common asks to exact tools. Live replay of the exact failing question ('what is the current post scheduled') now answers with the full calendar in ONE turn: 40 items, next post #2791 in ~21 min, 09:00/13:00/20:00 slots. 556/556, deployed.
2026-08-30 02:45 — QA loop ITERATION 54 (verified-clean pass; suite 556/556, zero failures/errors): full-app-surface deploy booted clean, smoke green 7/7 (one slow boot at 21s — X reads after restart), zero errors, quiet night. Clean pass.
2026-08-30 04:20 — TIER-2 CAPABILITIES shipped (answer to 'what else should the agent be capable of'): 6 new tools (31 total) — remix_draft (repurpose any draft: thread/shorter/longer/EN/AR/question/story), draft_variants (N versions, all saved), get_digest (on-demand report), reply_to_mention (specific mention replies from chat), competitor_scan (study any handle's timeline), reflect_now (learn on command). Remaining known gaps documented for later: trend-watch alerts (needs scheduler integration), image generation (no free tier yet). 560/560, deployed.
2026-08-30 05:30 — THE FULL BUILD shipped (owner: 'build them all and stop the current loop' — loop stopped): (1) PRECISION MODE — create capped at 2/run, each draft pre-scored against learned rejection rules (owner data: 22% approval rate drove this); (2) MORNING BRIEFING 09:03 daily — the agent messages the owner first (overnight publishes + decision cards + schedule, live-rendered); (3) PERF A/B every 2h — winners journal the pattern, losers flag the format; (4) TREND WATCHES + thread continuation (33 tools); (5) RESILIENCE — X error taxonomy with fix-path alerts, cookie countdown (account 2: 8 days, renew now), nightly DB vacuum, keepalive daemon running. Also fixed the live TG 'show me' issue (4 summaries, never the posts — RENDER RULE + 200-char previews). 567/567.
2026-08-30 06:15 — 05:00 DAILY SESSION RESET shipped (owner: 'like Hermes, every morning at 5am'): reset_sessions sets a DB watermark + clears TG windows; both chat_history queries filter by it. Yesterday's conversation stays in the DB but leaves the live context. The BRAIN (rules, dialect, rejection learning) is deliberately NOT reset — only the conversational window. SQLite format gotcha (ISO-T vs space-T) caught and fixed before deploy. 569/569, deployed.
2026-08-30 07:00 — SHOW MODE shipped (the 'show me the scheduled posts' saga ends): the RENDER RULE was in the first-turn prompt but the followup turn (where tool results land) was still in report-mode — '40 items total' instead of the posts, twice. _followup now detects show/list/where-is (EN+AR) and switches to SHOW MODE: every item rendered with id, time, full text; counts forbidden. Live replay: 3586-char formatted table of all 40 upcoming posts. 569/569, deployed.
2026-08-30 05:15 — QA loop RESTARTED (job a2f261fb, hourly at :07; hard rule embedded: NEVER publish on @Mr_CryptoYT). Iteration 1 verified-clean: 569/569, zero errors, smoke green, ZERO posts since the loop's watch began (published count since 04:00 = 0).
2026-08-30 06:15 — QA loop iteration 2 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓ (published since 05:00 = 0 — the @Mr_CryptoYT rule holds). Quiet pre-dawn, nothing needed. Clean pass.
2026-08-30 07:15 — QA loop iteration 3 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓ (0 published). Clean pass.
2026-08-30 08:15 — QA loop iteration 4 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓ (0 published). KEEPALIVE'S FIRST REAL CATCH: the server died overnight and the crash monitor detected it (down 2 checks) + restarted it at 07:15 — scheduler, TG poller, and learn loop all resumed cleanly. The robustness tier proving itself.
2026-08-30 09:15 — QA loop iteration 5: ZERO-POST CHECK ✓ (0 published). BUG FIXED: the trend watch's first live check crashed — check_watches called websearch.x_search_query (nonexistent; real name x_search). The hourly watch had been logging the failure without ever searching. Fixed + verified live (1 watch checked, 0 alerts — correct threshold behavior). Study loop also ran clean (+52 niche posts, hooks bank 20). 569/569.
2026-08-30 10:15 — QA loop iteration 6: VIOLATION DETECTED AND FIXED. Draft #2758 shipped on @Mr_CryptoYT at 09:01 (yesterday's approval hitting its scheduled slot — the rule was policy, not code). REMEDIATION: (1) all 14 remaining approved drafts on acct 2 flipped to pending — nothing ships without re-approval; (2) PUBLISH KILL SWITCH wired into the publish loop: publish_paused_{acct} checked FIRST, returns immediately (kills posts, link replies, everything) — set for account 2; clear to re-enable. Also noted: the morning briefing fired on time (09:03), keepalive working, watch running clean after its fix. 569/569.
2026-08-30 14:15 — QA loop iteration 7: ZERO-POST CHECK ✓ (0 published since kill switch; every publish cycle correctly KILLED). BUG FIXED: the trend watch's hourly check raised 'asyncio.run() cannot be called from a running event loop' — x_search calls asyncio.run internally and the scheduler is already in a loop. check_watches now async, x_search via to_thread (same fix class as the 08-24 TG x_trends). The watch should finally search on its next hourly pass. 569/569.
2026-08-30 15:15 — QA loop iteration 8 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓ (0 published since kill switch). Watch hasn't fired its hourly pass yet since the async fix deploy — verification continues next pass. Clean.
2026-08-30 16:15 — QA loop iteration 9 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓ (0 published since kill switch). WATCH VERIFIED WORKING: the async fix resolved the loop conflict — manual async check confirms 1 watch checked, 0 alerts (correct: 'AI agents' topic below the 3-fresh-post threshold right now; check_watches only logs alerts/errors, so quiet = healthy). Clean pass.
2026-08-30 17:15 — QA loop iteration 10 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓ (0 published since kill switch). Quiet late-afternoon. Clean pass.
2026-08-30 18:15 — QA loop iteration 11 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓. Clean pass.
2026-08-30 19:15 — QA loop iteration 12 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓. Clean pass.
2026-08-30 20:15 — QA loop iteration 13 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓. Clean pass.
2026-08-30 21:15 — QA loop iteration 14 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓. Clean pass.
2026-08-30 22:15 — QA loop iteration 15 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓. Clean pass.
2026-08-30 23:15 — QA loop iteration 16 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓. Clean pass.
2026-08-31 00:15 — QA loop iteration 17 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓. Clean pass.
2026-08-31 01:15 — QA loop iteration 18 (verified-clean; 569/569, zero errors): ZERO-POST CHECK ✓. Clean pass.
2026-08-31 02:15 — NUMBERS LIE fixed (owner: 'he is lying about numbers'): get_schedule was returning one total count and the model labeled it 'today' — '26 total today' when 26 was the whole calendar (past + future + rejected). The tool now returns explicitly labeled buckets (today/upcoming/past/status_breakdown) with per-bucket notes the model cannot conflate. Rejected drafts excluded entirely. 569/569, deployed.
2026-08-31 00:35 — STRANDED VISIBILITY fixed (found while live-testing the numbers fix): the publish kill switch's early return sat ABOVE _alert_stranded — while acct 2 was frozen, the alert path never ran, so 4 fresh stranded drafts on acct 1 (#2426-#2433) got zero alerts for two days. Same blind spot in get_schedule: it reported an empty calendar while 11 approved drafts sat on acct 1. _alert_stranded now runs BEFORE the switch (the switch stops posting, never visibility) and get_schedule carries an other_accounts bucket (approved + due_now per account) with a note the model cannot ignore. Verified live at the 00:30 publish tick: stranded alert fired first, then the kill line. 571/571, deployed. Note for owner: those 11 acct-1 drafts (#2386-#2433) need a decision — switch active account to 1 to let them ship, or reject them; they include 2 due TODAY (09:00 #2428, 13:00 #2433).
2026-08-31 00:40 — QA loop iteration 19 (verified-clean; 571/571, zero errors): ZERO-POST CHECK ✓ (0 published on acct 2 since kill switch; switch confirmed set). Fixes from last pass verified live: 00:30 publish tick fired the stranded alert BEFORE the kill line (correct order). Engage loop healthy (2 approval-gated replies drafted, nothing can ship). Keepalive daemon alive. Briefing counting audited — it filters approved+today per account explicitly, no numbers-lie class bug there. OPEN: 11 acct-1 drafts still await the owner's decision (#2428 due 09:00 today).
2026-08-31 01:45 — QA iteration 20 — TIMESTAMPS fixed (root cause of every 'lying numbers' report): posts.created_at stored X's raw 'Wed Sep 24 14:10:11 +0000 2025' alongside ISO — lexically 'W' > '2', so EVERY time filter (week/month/all) matched EVERY post. 'Best post this week' returned lifetime totals (820 posts, 64.3M impressions) verbatim; owner: 'you are lying'. ORDER BY created_at also put the pre-pivot era first, starving own_posts(500)/voice profile of the newest posts. Fixes: _norm_created_at converts any X shape to local-naive ISO at the upsert boundary; v0.5.x migration converted all 800 legacy rows in place (verified: 0 non-ISO rows remain; week=34 posts/309K impressions, month=108/14.7M, all=820/64.3M — timeframes finally differ); watch._hours_old reuses the converter (its ISO-only parser scored live search results 999h old — trend watches could NEVER alert outside dryrun). Also: analytics best_post excludes retweets ('your best post' must be your writing — the all-time best was an RT of GoogleDeepMind). 575/575, deployed. OPEN: 11 acct-1 drafts still await the owner (#2428 due 09:00).
2026-08-31 02:35 — QA iteration 21 (verified-clean; 575/575): HARD RULE holds — 0 published on acct 2 since the kill switch (the ad-hoc check briefly reported 2; both pre-dated the switch — #2758 IS the 09:01 violation that created it, #2801 shipped 03:57 that night. My query hit the exact mixed-format trap iteration 20 fixed: space cutoff vs T-format published_at). Zero-post check is now CODE (scripts/qa_zero_post.py, T-vs-T only, exit 1 on real violation) so the QA query can never drift again. Loops healthy: create +1 draft, trend-scout #2859 (both acct 2, approval-gated), smoke green 7/7, no errors/warns. OPEN: 11 acct-1 drafts still await the owner (#2428 due 09:00).
2026-08-31 03:35 — QA iteration 22 (verified-clean; 575/575): HARD RULE ✓ (script: 0 published, switch on). MILESTONE: the 'AI agents' trend watch fired its FIRST production alert at 02:38 (threshold crossed, last_alert stamped) — the full chain works after the asyncio fix (iter 7) and the _hours_old timestamp fix (iter 20); before those, every hourly check either crashed or scored all results ancient. Study loop healthy (+55 posts, hooks bank 20), smoke green 7/7, no errors/warns. OPEN: 11 acct-1 drafts still await the owner (#2428 due 09:00).
2026-08-31 04:45 — QA iteration 23 — KEEPALIVE BLIND SPOT fixed: at 04:20 the nightly vacuum + whisper warm blocked the server loop ~2.5 min; health checks timed out; keepalive's replacement died on bind error 10048 (the hung process held 7878). The server self-recovered and the 04:30 ticks proved its scheduler survived, but a permanent hang would have stayed down forever — keepalive faithfully restarting into a wall. _restart_server now taskkills the port holder first. Also: keepalive is now singleton (lock + pid liveness + race re-read) after discovering the runtime shim mirrors python children — two monitors would taskkill each other's fresh servers mid-outage. Nightly chain otherwise healthy (vacuum 5.3MB, rejection reflection +3 rules from 12 drafts, metrics 20 posts — one handled X read timeout). HARD RULE ✓ (script). 579/579. OPEN: 11 acct-1 drafts, #2428 due 09:00.
2026-08-31 05:30 — QA iteration 24 (verified-clean; 579/579): HARD RULE ✓ (script). First live 05:00 session reset fired on schedule (watermark stamped, both frontends start fresh — Hermes-style morning boundary works). Smoke green 7/7, no errors/warns. OPEN: 11 acct-1 drafts, #2428 due 09:00.
2026-08-31 06:35 — QA iteration 25 — EVENT-LOOP STALLS fixed at the root: second overnight keepalive restart (06:18, during the learn phase — metrics 60 posts + whisper voice rebuild). The port-clear fix worked exactly as designed (clean restart, smoke green 8s later, no bind error), but the root was build_voice running ON the loop — the only heavy sync call not yet in to_thread. Both call sites (scan, learn) now thread-wrapped. First restart's fix recovered the crash; this one stops tripping it. HARD RULE ✓ (script). One transient X read (handled, retry). 579/579, deployed. OPEN: 11 acct-1 drafts, #2428 due 09:00 (~2.5h).
2026-08-31 07:30 — QA iteration 26 (verified-clean; 579/579): HARD RULE ✓. WHISPER FIX VERIFIED LIVE: the learn phase re-ran at 07:18 (autopilot retried after the 06:18 restart killed it mid-flight) and completed 4.5 min of metrics+voice+brain with zero keepalive intervention — the exact workload that stalled the loop at 06:18. Voice rubric rebuilt (ar/en/mixed, 8 examples), brain +2 rules. Create loop +1 draft, trend-scout #2863 (acct 2, approval-gated). No errors/warns. OPEN: 11 acct-1 drafts, #2428 due 09:00.
2026-08-31 08:30 — QA iteration 27 (verified-clean; 579/579): HARD RULE ✓ (script; every 10-min publish tick correctly killed). Study +56 posts (hooks bank 20), engage drafted 2 approval-gated replies, smoke green, zero errors/warns. Steady state overnight-to-morning. #2428 due 09:00 — its one-time stranded alert will fire then; briefing 09:03. Owner decision on the 11 acct-1 drafts still pending.
2026-08-31 09:30 — QA iteration 28 (verified-clean; 579/579): HARD RULE ✓. The 09:00 boundary executed exactly as designed: #2428's one-time stranded alert fired the moment it came due (alert-before-kill order correct, dedupe stamped), morning briefing pushed at 09:03 — the owner's TG now carries the full decision picture for the 11 acct-1 drafts (#2433 at 13:00 is the last un-alerted one). All loops green otherwise. No owner decision yet.
2026-08-31 10:30 — QA iteration 29 (verified-clean; 579/579): HARD RULE ✓. Quiet pass — no errors, loops green (engage 2 approval-gated replies, mentions 0, smoke green). No owner decision on the acct-1 queue yet; #2433 alerts at 13:00 if still parked.
2026-08-31 11:30 — QA iteration 30 (verified-clean; 579/579): HARD RULE ✓. Second learn phase since the whisper fix ran 3.5 min stall-free (fix holding). No errors, no new chat. #2433 alerts at 13:00 if still parked.
2026-08-31 12:30 — QA iteration 31 (verified-clean; 579/579): HARD RULE ✓ — note the owner is active on TG: approved #2876 (12:32) + #2877 (13:20) on acct 2; with the kill switch on they will sit due-but-blocked at slot time (publish returns KILLED, nothing ships) until the owner clears publish_paused_2. Agent's live reply cited rejection rule R12 (crypto news worst performer) — rejection learning visible in production. No errors.
2026-08-31 13:45 — QA iteration 32 — STRANDED ALERT STARVATION fixed: #2433 came due at 13:00 and never alerted. Two compounding bugs: (1) the LIMIT 10 window returned the 10 OLDEST due drafts — all already in the dedupe set — so an 11th straggler could never enter (at 09:00 #2428 had fired only because it happened to be exactly 10th); (2) the dedupe update marked every FETCHED draft as alerted, silently swallowing anything beyond the 10/pass cap without telling anyone. Window → 50, only actually-alerted drafts enter the dedupe set. Verified live: the first post-deploy tick fired #2433's missing alert and the dedupe set is complete. 580/580. HARD RULE ✓ (also proven under load at 12:32 when owner-approved #2876's slot passed blocked).
2026-08-31 14:30 — QA iteration 33 (verified-clean; 580/580): HARD RULE ✓. Quiet pass — no errors, no new chat, smoke green. All parked drafts alerted; nothing new due.
2026-08-31 14:55 — QA iteration 34 (user report: 'see last messages, see why it have the error') — QUOTE FLOW fixed: 'quote it' at 14:39 failed twice. (1) The cookie client called c.get_tweet() — twikit 2.3.3 renamed it get_tweet_by_id. (2) The renamed method crashes INSIDE twikit on X's current payload (KeyError 'itemContent') — the plural get_tweets_by_ids parses fine and is now preferred (live-verified: real tweet fetched through the wrapper). (3) The retry had stalled on 'no URLs came back' — search results now carry a ready-built x.com URL (handle + id were always in the payload). 582/582, deployed. Owner can retry the AshCrypto quote on TG.
2026-08-31 15:30 — QA iteration 35 (verified-clean; 582/582): HARD RULE ✓. Quiet pass — no errors since the quote-flow deploy, no new chat yet (owner hasn't retried the quote).
2026-08-31 16:40 — QA iteration 36 — QUOTE RETRY STARVATION + REFLECT FRAGILITY fixed: the owner's 15:32 'Try now again' STILL failed with 'no tweet URL/ID' — the client-level url fix from iteration 34 never reached the model because _tool_x_search projected results down to text/author/likes. url + x_id now ride through the projection (with constructed fallback + a note naming them as the quote reference). Also: reflect(metrics) died at 15:45 on one malformed JSON emit (20000-token budget — not starvation); reflect now retries once at lower temperature and logs the FULL raw text (was a 200-char clip). Config default max_tokens 1200→4000 (defense for config-less deployments; live toml already 20000). Watch fired its 2nd production alert at 15:58. 583/583, deployed.
2026-08-31 17:45 — QA iteration 37 (user: 'check now its not good') — THREE stacked failures found: (1) the 17:23 'I want one Qoute' turn DIED SILENTLY — watchdog: empty stream reply, stop_reason=max_tokens, all-thinking. The chat builders' hard 4000 cap starves thinking-heavy turns; the toml's 20000 never reached them. All four builders now use max(cfg.llm.max_tokens, 4000). (2) At 17:19 the agent had the tweet URL but wrote FOUR plain posts with the link pasted in the body instead of one quote — quote routing added (ONE quote ask = ONE create_quote_draft; the url never appears in post text). (3) Verified the quote tool end-to-end with the loaded config: real quote draft created off @AshCrypto's tweet. 583/583, deployed.
2026-08-31 17:50 — QA iteration 38 (verified-clean; 583/583): HARD RULE ✓. Quiet pass — deployed fixes holding, no new errors, no chat activity since the quote fix landed (owner hasn't retried yet). Quote draft #2913 waits for approval.
2026-08-31 18:20 — QA iteration 39 (user: 'CHECK NOW FIX THE ISSUE') — root causes chained: the owner's 18:07 'Delete all drafts' (a clean-slate after the dup mess) ALSO wiped quote #2913 they still wanted; recreating it then failed with 'voice lock rejected (score 0, empty draft)' — GLM emitted a wrong JSON key, the reader found nothing. _draft_one now reads tweet/text/post, logs the keys it saw, and re-asks once. DELIVERED: quote draft #2915 (on-topic Japan/stablecoin, Iraqi, attached to @AshCrypto's tweet) awaiting /approve. Also noted: a vague `angle` param lets the topic drift — the angle must name the topic (routing note updated behaviorally via usage). 583/583, deployed.
2026-08-31 18:30 — QA iteration 40 (verified-clean; 583/583): HARD RULE ✓. Quiet pass — all fixes holding, no errors, no new chat. Quote #2915 pending approval.
2026-08-31 19:20 — QA iteration 41 (owner: 'i cant force it publish it — i want to publish scheduled posts from agent or UI') — OWNER-OVERRIDE PUBLISHING shipped, end to end. What was broken: 'Publish it' said three times at 18:39 while run_loop reported ok:true with killed buried inside — the agent narrated shipping, nothing moved; and NO override path existed (UI had /send for replies only). Now: publish_draft chat tool + POST /api/drafts/{id}/publish ship ONE draft immediately, beating the kill switch BY DESIGN (the switch stops the machine, never the owner). Injection safety preserved via a trust gate (contextvar armed only inside chat_reply/chat_reply_stream; the safety eval now verifies unarmed execution fails for every publish-capable tool). Two more twikit 2.3.3 drifts fixed on the way (quote_tweet_id → attachment_url, media_id → media_ids). LIVE-VERIFIED: quote #2915 PUBLISHED via the new endpoint at 19:15:25 → x.com/Mr_CryptoYT/status/2094474159883427851 (owner-directed; tripwire updated to distinguish owner posts from autonomous ones). 585/585.
2026-08-31 19:30 — QA iteration 42 (verified-clean; 585/585): HARD RULE ✓ (0 autonomous posts; the 1 owner-directed #2915 is the owner's explicit publish, marked legitimate by the tripwire). No errors, no new chat. Quiet pass post-deploy.
2026-08-31 20:35 — QA iteration 43 — TG GATE FIX: the owner's 19:33 'can u publish it' from their OWN Telegram was refused ('this isn't an owner chat turn') — chat_reply_tg_stream is a separate engine from chat.chat_reply_stream and never armed the trust gate. TG now arms like the web paths; the TG hard 4000 cap also became max(configured, 4000). Verified by regression test through the full TG stream (publish action executes, draft publishes). Also confirmed the 19:33 agent reply honestly reported the refusal instead of narrating success. HARD RULE ✓ (0 autonomous). 586/586, deployed.
2026-08-31 21:30 — QA iteration 44 (verified-clean; 586/586): HARD RULE ✓ (0 autonomous; 1 owner-directed). No errors, no new chat. Queue: 3 replies pending-approval tonight (#2916-2918, slots 19:19/20:22/21:21 already passing — they wait for unpause or owner command), 2 acct-1 posts still parked from the stranded batch.
2026-08-31 21:50 — QA iteration 45 (owner: 'the voice to text is so bad') — TRANSCRIPTION OVERHAUL: the voice pipeline ran whisper TINY with beam 1 — hopeless for Iraqi Arabic. Now medium (env-tunable via OPENSTANLEY_WHISPER_MODEL), beam 5, initial_prompt biased to Iraqi Arabic + English crypto/AI code-switching, typing indicator during CPU transcription, model pre-downloaded (142s one-time). 587/587, deployed.
2026-08-31 22:45 — QA iteration 46 (owner: 'i asked for long post it still dont do it... the ai agent should know that') — FALSE CEILING fixed: the agent told the owner '277/280 is the physical ceiling, X will reject anything longer' while the account is X PREMIUM (25k-char posts, owner-confirmed). max_post_chars() now carries the real per-account capability (x_premium setting, set for acct 2); the chat prompts state CONTENT CAPABILITY explicitly ('long' = ONE long-form post, never a forced thread, never a false ceiling); voice-lock band + watch cap follow it. Long-form draft delivered for approval. 588/588, deployed.
2026-08-31 22:50 — QA iteration 47 (verified-clean; 588/588): HARD RULE ✓ (0 autonomous; 1 owner-directed). No errors, no new chat. Premium long-post capability deployed; draft #2934 (1010 chars) awaits the owner's /approve.
2026-08-31 23:20 — QA iteration 48 (owner: 'the voice transcribing still bad') — MAX TRANSCRIPTION: no voice note had actually reached the server since the medium deploy (zero 'heard:' echoes — the verdict predated the upgrade), but went to the ceiling anyway: large-v3-turbo (pre-downloaded, 151s one-time) + a constrained LLM repair pass that restores Iraqi dialect from whisper's MSA normalization (never translates/adds/drops; runaway repairs fall back to raw). 589/589, deployed.
2026-08-31 23:30 — QA iteration 49 (verified-clean; 589/589): HARD RULE ✓. Premium long-post flow verified in live traffic: the owner asked for long posts at 23:08-23:17 and the agent delivered long-form drafts and iterated edits (title swap, اصبار→صبر) — no 280 ceiling, no forced threads. A re-publish attempt on the already-live #2915 was correctly refused by the already-published guard (no duplicate post). No errors.
2026-09-01 00:30 — QA iteration 50 (verified-clean; 589/589): HARD RULE ✓. One transient X search 404 at 00:18 hit the hourly watch (retried, skipped gracefully — next pass retries; the same flake class seen at 18:04). No voice notes since the turbo deploy (owner hasn't tested yet).
2026-09-01 01:00 — QA iteration 51 (owner: calendar flexibility) — CALENDAR ITEM UPGRADE: the slot popover was read-only text + timestamp. Now one click on a scheduled post shows the full text (RTL-aware), the slot justification (scheduled_reason: 'metrics peak 07:00, 12h after last post' style), inline EDIT with save/cancel + live char count, and PUBLISH NOW wired to the owner-override endpoint (opens the post on X after publishing). AR+EN strings. Frontend built + live. 4/4 calendar tests.
2026-09-01 01:45 — QA iteration 52 — TRENDS TURNS STALLING fixed: two consecutive 'suggest trends' turns burned the 300s handler cap while X reads flaked (the same 404-code-34 family hitting the watch). x_search/x_trends chat tools now run under a 45s wall-clock cap and return a fail-fast error naming web_search as the fallback — the model routes around a slow X within the turn. HARD RULE ✓. 589/589, deployed.
2026-09-01 02:05 — QA iteration 53 (owner, three mid-pass asks) — CALENDAR ROUND 2: queue drafts open the same detail popover (read/edit/publish); reply items show WHO they reply to (↩ @handle chip on the card + popover header — reply_to now flows through the calendar API from mention- and niche-reply metadata; 23 reply items verified live); every scheduled card shows its exact time in the card header. Built + live.
2026-09-01 02:40 — QA iteration 54 (verified-clean; 590/590): HARD RULE ✓. Clean pass — all night's UI + agent work deployed and holding (calendar detail/edit/publish/remove/links/colors, strategy theses fixed + live-filled with 4 real engagement-backed theses, Ideas Write now drafts the clicked idea). No errors, no new chat.
2026-09-01 02:45 — QA iteration 55 (owner: 'strategy generate — 30-40s then nothing') — the one-pager generator starved on a hard 1800-token cap (the same GLM thinking-phase starvation fixed across chat/TG tonight): empty reply → RuntimeError → HTTP 500. Budget now max(configured, 4000). LIVE-VERIFIED: force-generate returned a 4,321-char strategy grounded in the account's real data (positioning: the default Arabic AI-builder account, citing the film studio, Seedance cost math, Mac Mini take). Strategy section now fully operational end to end.
2026-09-01 03:00 — QA iteration 56 — STRATEGY BLUEPRINT (owner's spec): the one-pager now generates in the creator-strategy shape — Strategic Goal with concrete follower targets (43,713 → 65k by Mar 2027 → 90k by Sep 2027, derived from the four proven engagement patterns), Primary/Secondary/Others audience tiers inferred from real engagement, first-person positioning line, five pillars weighted 25/25/20/15/15 (=100, weighted by actual performance) each with Core Message / What I Share / Why It Matters, Posting Rhythm from measured best hours, and Notes (Lean Into / Avoid) with Avoid grounded in learned rejection rules. The account data now feeds working theses + active rules + best hours + language mix. 590/590, live-verified (6,277 chars).
